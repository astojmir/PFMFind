** REQUIREMENTS FOR PFMFind  **

This is an environment based on Python 2.5.6.

Numeric==24.2
Pmw==1.3.2
biopython==1.42
egenix-mx-base==2.0.6
psycopg2==2.0b8
transcendental==0.05

After applying new bug fixes, the GUI ought to work.

NOT TESTED YET: creating indexes, searching.

** HITS VIEW TOO SLOW? -> CREATE INDEXES in Postgres database **

CREATE INDEX biosequence_pkey ON biosequence (bioentry_id);
CREATE INDEX bioentry_accession_key ON bioentry (accession);
CREATE INDEX taxon_name_taxid ON taxon_name (taxon_id);

For index creation, add:

CREATE INDEX bioentry_pk_ix ON bioentry (bioentry_id);


** KEYWORD VIEW TOO SLOW? -> CREATE INDEXES in Postgres database **

CREATE INDEX bioentry_id_ix ON seqfeature (bioentry_id);
CREATE INDEX term_id_ix ON term (term_id);
CREATE INDEX seqfeature_id_ix ON location (seqfeature_id);

But, the query with outer join may be too slow anyway.
